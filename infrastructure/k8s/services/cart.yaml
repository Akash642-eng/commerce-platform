apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart-service
  namespace: default          # FIX Bug#1: explicit namespace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cart-service
  template:
    metadata:
      labels:
        app: cart-service
    spec:
      containers:
        - name: cart-service
          image: cart-service:v2
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secret
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
          livenessProbe:
            httpGet:
              path: /health      # FIX Bug#6: was /
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health      # FIX Bug#6: was /
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 5

---

apiVersion: v1
kind: Service
metadata:
  name: cart-service
  namespace: default            # FIX Bug#1: explicit namespace
spec:
  type: ClusterIP               # FIX Bug#2: was NodePort
  selector:
    app: cart-service
  ports:
    - port: 8000
      targetPort: 8000
